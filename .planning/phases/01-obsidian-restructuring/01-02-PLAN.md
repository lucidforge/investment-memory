---
phase: 01-obsidian-restructuring
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["vault/产业趋势/", "vault/投资教训/", "vault/产业趋势/_index.md", "vault/投资教训/_index.md"]
autonomous: true
requirements: []

must_haves:
  truths:
    - "Industry trends are converted to individual Obsidian .md files with actual values (not '未知')"
    - "Investment lessons are converted to individual Obsidian .md files with correct wikilinks"
    - "Each trend has complete frontmatter extracted from nested basic_info fields"
    - "Each lesson has complete frontmatter with correct related_crisis ID"
    - "Both categories have _index.md summary files"
    - "Trends and lessons use tags and wikilinks for relations"
  artifacts:
    - path: "vault/产业趋势/*.md"
      provides: "Individual trend notes (6 files)"
    - path: "vault/投资教训/*.md"
      provides: "Individual lesson notes (3 files)"
    - path: "vault/产业趋势/_index.md"
      provides: "Trends summary index"
    - path: "vault/投资教训/_index.md"
      provides: "Lessons summary index"
  key_links:
    - from: "skills/memory/assets/memory/industry_trends/trends/*.json"
      to: "vault/产业趋势/*.md"
      via: "JSON to Obsidian Markdown conversion (basic_info.* nested fields)"
    - from: "skills/memory/assets/memory/lessons_learned/lessons/*.json"
      to: "vault/投资教训/*.md"
      via: "JSON to Obsidian Markdown conversion (related_crisis ID correction)"
---

## Objective

Convert industry trends and investment lessons from JSON to Obsidian Markdown format, with fixes for data loss and broken wikilinks identified in review.

**Purpose:** Complete the knowledge base content by converting all trend and lesson data to human-readable Obsidian notes. Addresses three HIGH-severity review findings:
1. Trend conversion must extract nested `basic_info.*` fields (not top-level)
2. Lesson wikilinks must use correct crisis IDs (CRISIS_2026 not CRISIS_2025)
3. File counts must match reality (6 trends, 3 lessons — not 11)

**Output:**
- 6 industry trend .md files with correct frontmatter values
- 3 investment lesson .md files with correct wikilinks
- _index.md files for both categories

## Context

@.planning/phases/01-obsidian-restructuring/01-CONTEXT.md
@.planning/phases/01-obsidian-restructuring/01-REVIEWS.md

**Source Data (actual file counts):**
- Industry trends: `skills/memory/assets/memory/industry_trends/trends/*.json` (6 files: EV, Carbon, Metaverse, AI, Autonomous, Quantum)
- Investment lessons: `skills/memory/assets/memory/lessons_learned/lessons/*.json` (3 files)

**Critical JSON Structure (from review):**
Trend JSON fields are NESTED under `basic_info`:
```json
{
  "trend_id": "TREND_2023_AI",
  "trend_name": "AI/大模型革命",
  "basic_info": {
    "start_year": 2023,        // ← NOT top-level start_year
    "current_phase": "growth", // ← NOT top-level current_phase
    "trigger_event": "...",    // ← NOT top-level trigger_event
    "keywords": [...]
  }
}
```

Lesson JSON has WRONG year in related_crisis:
```json
{
  "related_crisis": "CRISIS_2025_USIRAN"  // ← Should be CRISIS_2026_USIRAN
}
```
Actual crisis file: `skills/memory/assets/memory/crisis_knowledge/events/CRISIS_2026_USIRAN.json`

**Decisions Implemented:**
- D-02: Complete frontmatter (all JSON fields preserved)
- D-03: Tags for categorization, wikilinks for relations
- D-05: Token optimization with collapsible callouts

## Tasks

<task type="auto" tdd="true">
<name>Task 1: Convert industry trends to Obsidian Markdown</name>
<files>vault/产业趋势/*.md, vault/产业趋势/_index.md</files>
<read_first>
- skills/memory/assets/memory/industry_trends/trends/TREND_2023_AI.json (example — note nested basic_info structure)
- All 6 JSON files in skills/memory/assets/memory/industry_trends/trends/
- .planning/phases/01-obsidian-restructuring/01-CONTEXT.md (decisions D-02, D-03, D-05)
</read_first>
<behavior>
- Each of the 6 JSON files becomes one .md file with trend_id as filename
- Frontmatter extracts start_year, current_phase, trigger_event from NESTED basic_info object
- Content uses collapsible callouts for detailed sections
- Tags use #产业趋势 and #current_phase patterns
- Lifecycle stage clearly indicated with actual values (not "未知")
</behavior>
<action>
For each JSON file in `skills/memory/assets/memory/industry_trends/trends/` (6 files total):

1. Read the JSON file completely
2. **CRITICAL: Extract nested basic_info fields correctly:**
   - `start_year` = `json.basic_info.start_year` (NOT `json.start_year`)
   - `current_phase` = `json.basic_info.current_phase` (NOT `json.current_phase`)
   - `trigger_event` = `json.basic_info.trigger_event` (NOT `json.trigger_event`)
   - `keywords` = `json.basic_info.keywords` (NOT `json.keywords`)

3. Create `vault/产业趋势/{trend_id}.md` with this exact template:

```markdown
=== (frontmatter delimiter)
trend_id: "{trend_id}"
trend_name: "{trend_name}"
type: trend
start_year: {basic_info.start_year}
current_phase: "{basic_info.current_phase}"
tags:
  - 产业趋势
  - {basic_info.current_phase}
aliases:
  - {trend_name}
keywords: {basic_info.keywords array}
trigger_event: "{basic_info.trigger_event}"
=== (frontmatter delimiter)

# {trend_name}

> [!info] 趋势概况
> - **起始年份**: {basic_info.start_year}
> - **当前阶段**: {basic_info.current_phase}
> - **触发事件**: {basic_info.trigger_event}
> - **关键词**: {basic_info.keywords joined with ", "}

## 趋势详情

> [!details]- 详细描述
> {trend_details from JSON}

## 市场影响

### 美股
> [!details]- 美股影响
> {market_impact.us_stock.content from JSON}

### A股
> [!details]- A股影响
> {market_impact.a_stock.content from JSON}

### 港股
> [!details]- 港股影响
> {market_impact.hk_stock.content from JSON}

## 投资机会

### 短期
{investment_opportunities.short_term items as list}

### 中期
{investment_opportunities.medium_term items as list}

### 长期
{investment_opportunities.long_term items as list}

## 教训

{lessons items as list}

## 当前状态

> [!tip] 状态监控
> - **阶段**: {current_status.phase}
> - **关注指标**: {current_status.key_watch joined with ", "}
> - **风险因素**: {current_status.risk_factors joined with ", "}
```

4. Create `vault/产业趋势/_index.md`:
```markdown
=== (frontmatter delimiter)
title: 产业趋势索引
type: index
tags:
  - 索引
=== (frontmatter delimiter)

# 产业趋势知识库

收录产业趋势及其生命周期分析。

| 趋势ID | 名称 | 起始年 | 当前阶段 | 触发事件 |
|---------|------|--------|----------|----------|
{one row per trend: | {trend_id} | {trend_name} | {basic_info.start_year} | {basic_info.current_phase} | {basic_info.trigger_event} |}
```
</action>
<acceptance_criteria>
- vault/产业趋势/ contains exactly 6 .md files (one per JSON file)
- Each .md frontmatter has start_year with actual year (2019, 2020, 2021, 2023, 2024, 2025) — NOT "未知" or "unknown"
- Each .md frontmatter has current_phase with actual phase (e.g., "growth", "decline") — NOT "未知"
- Each .md frontmatter has trigger_event with actual event description — NOT "未知"
- vault/产业趋势/_index.md exists and contains a table with 6 data rows
- grep "未知" vault/产业趋势/*.md returns no matches
</acceptance_criteria>
<verify>
<automated>ls vault/产业趋势/*.md 2>/dev/null | wc -l | xargs test 6 -eq && ! grep -l "未知" vault/产业趋势/*.md 2>/dev/null && echo "PASS" || echo "FAIL"</automated>
</verify>
<done>All 6 trend JSON files converted to Obsidian Markdown with correct nested field extraction</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Convert investment lessons to Obsidian Markdown</name>
<files>vault/投资教训/*.md, vault/投资教训/_index.md</files>
<read_first>
- skills/memory/assets/memory/lessons_learned/lessons/LESSON_20260325_5774c4b6.json (example — note wrong related_crisis year)
- All 3 JSON files in skills/memory/assets/memory/lessons_learned/lessons/
- vault/危机事件/ (to verify correct crisis IDs exist)
- .planning/phases/01-obsidian-restructuring/01-CONTEXT.md (decisions D-02, D-03)
</read_first>
<behavior>
- Each of the 3 JSON files becomes one .md file with lesson_id as filename
- Frontmatter contains: lesson_id, type, related_crisis, asset, lesson_date, tags, aliases
- Related crisis referenced via wikilinks with CORRECT year (CRISIS_2026 not CRISIS_2025)
- Tags use #投资教训 and #asset patterns
</behavior>
<action>
For each JSON file in `skills/memory/assets/memory/lessons_learned/lessons/` (3 files total):

1. Read the JSON file completely
2. **CRITICAL: Fix related_crisis ID if year is wrong:**
   - If `related_crisis` is `CRISIS_2025_USIRAN`, change to `CRISIS_2026_USIRAN` (actual file is CRISIS_2026_USIRAN.json)
   - Verify the corrected ID exists in `skills/memory/assets/memory/crisis_knowledge/events/`

3. Create `vault/投资教训/{lesson_id}.md` with this exact template:

```markdown
=== (frontmatter delimiter)
lesson_id: "{lesson_id}"
type: lesson
related_crisis: "[[{corrected_related_crisis}]]"
asset: "{judgment.asset}"
lesson_date: "{date}"
tags:
  - 投资教训
  - {judgment.asset}
aliases:
  - "{date} {judgment.asset}教训"
judgment_direction: "{judgment.direction}"
confidence: {judgment.confidence}
actual_change: "{actual_result.actual_change}"
=== (frontmatter delimiter)

# {date} {judgment.asset}投资教训

## 判断

- **资产**: {judgment.asset}
- **方向**: {judgment.direction}
- **置信度**: {judgment.confidence}

## 实际结果

- **实际变动**: {actual_result.actual_change}

## 错误分析

> [!danger] 根本原因
> {error_analysis.root_cause}

## 教训总结

{lessons_learned items as list}

## 规避策略

> [!tip] 未来建议
> {avoidance_strategy}

## 相关危机

[[{corrected_related_crisis}]]
```

4. Create `vault/投资教训/_index.md`:
```markdown
=== (frontmatter delimiter)
title: 投资教训索引
type: index
tags:
  - 索引
=== (frontmatter delimiter)

# 投资教训知识库

收录投资错误判断及其教训。

| 教训ID | 日期 | 资产 | 方向 | 实际结果 | 根本原因 |
|--------|------|------|------|----------|----------|
{one row per lesson: | {lesson_id} | {date} | {judgment.asset} | {judgment.direction} | {actual_result.actual_change} | {error_analysis.root_cause} |}
```
</action>
<acceptance_criteria>
- vault/投资教训/ contains exactly 3 .md files (one per JSON file)
- LESSON_20260325_5774c4b6.md contains "CRISIS_2026_USIRAN" (NOT "CRISIS_2025_USIRAN")
- No .md file contains "CRISIS_2025_USIRAN" (the incorrect ID)
- Each .md has frontmatter with related_crisis as wikilink format "[[CRISIS_...]]"
- vault/投资教训/_index.md exists with 3 data rows
</acceptance_criteria>
<verify>
<automated>ls vault/投资教训/*.md 2>/dev/null | wc -l | xargs test 3 -eq && ! grep -l "CRISIS_2025" vault/投资教训/*.md 2>/dev/null && grep -l "CRISIS_2026_USIRAN" vault/投资教训/LESSON_20260325_5774c4b6.md 2>/dev/null && echo "PASS" || echo "FAIL"</automated>
</verify>
<done>All 3 lesson JSON files converted to Obsidian Markdown with corrected wikilink IDs</done>
</task>

## Verification

- [ ] All 6 industry trend JSON files have corresponding .md files (not 11)
- [ ] All 3 investment lesson JSON files have corresponding .md files
- [ ] Each trend .md has actual start_year, current_phase, trigger_event values (not "未知")
- [ ] Each lesson .md has correct related_crisis wikilink (CRISIS_2026 not CRISIS_2025)
- [ ] `_index.md` files created for both categories
- [ ] Tags and wikilinks properly formatted

## Success Criteria

- All 6 trends converted with actual nested basic_info values (D-02) — review fix
- All 3 lessons converted with corrected crisis wikilinks (D-03) — review fix
- File counts match reality (6 trends + 3 lessons = 9 files, not 11) — review fix
- Both categories use collapsible callouts for token optimization (D-05)
- Cross-references established between lessons and related crises (D-03)

## Output

After completion, create `.planning/phases/01-obsidian-restructuring/01-02-SUMMARY.md`
