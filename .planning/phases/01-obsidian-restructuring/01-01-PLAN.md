---
phase: 01-obsidian-restructuring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["vault/", "vault/危机事件/", "vault/产业趋势/", "vault/投资教训/", "vault/投资框架/", "vault/危机事件/_index.md"]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vault folder structure exists with 4 category folders"
    - "Each folder contains an _index.md summary file"
    - "Crisis events are converted to individual Obsidian .md files"
    - "Each crisis event has complete frontmatter with all JSON fields"
    - "Crisis events use tags for categorization"
    - "Crisis events use wikilinks for related event references"
  artifacts:
    - path: "vault/危机事件/_index.md"
      provides: "Crisis events summary index"
    - path: "vault/危机事件/*.md"
      provides: "Individual crisis event notes"
  key_links:
    - from: "skills/memory/assets/memory/crisis_knowledge/events/*.json"
      to: "vault/危机事件/*.md"
      via: "JSON to Obsidian Markdown conversion"
---

## Objective

Create the Obsidian vault folder structure and convert all crisis events from JSON to Obsidian Markdown format.

**Purpose:** Establish the foundation for the investment memory knowledge base in Obsidian, with crisis events as the first content category.

**Output:**
- `vault/` directory with 4 category folders
- All crisis events converted to Obsidian .md files with complete frontmatter
- Crisis events _index.md with summary of all events

## Context

@.planning/phases/01-obsidian-restructuring/01-CONTEXT.md

**Source Data:**
- Crisis events: `skills/memory/assets/memory/crisis_knowledge/events/*.json` (15 files)
- Crisis index: `skills/memory/assets/memory/crisis_knowledge/index.json`

**Decisions Implemented:**
- D-01: Single vault with folder categories
- D-02: Complete frontmatter (all JSON fields preserved)
- D-03: Tags for categorization, wikilinks for relations
- D-05: Token optimization with collapsible callouts

**Review Notes (from 01-REVIEWS.md):**
- Crisis event conversion was rated as the strongest plan — no changes needed
- Directory naming is `vault/` (not `.vault/`) — confirmed correct in this plan

## Tasks

<task type="auto">
<name>Task 1: Create vault folder structure</name>
<files>vault/, vault/危机事件/, vault/产业趋势/, vault/投资教训/, vault/投资框架/</files>
<action>
1. Create `vault/` directory as the Obsidian vault root
2. Create 4 category subdirectories:
   - `vault/危机事件/` - Crisis events
   - `vault/产业趋势/` - Industry trends
   - `vault/投资教训/` - Investment lessons
   - `vault/投资框架/` - Investment frameworks
3. Verify directories exist
</action>
<verify>
<automated>test -d vault/危机事件 && test -d vault/产业趋势 && test -d vault/投资教训 && test -d vault/投资框架 && echo "PASS" || echo "FAIL"</automated>
</verify>
<done>All 4 category folders created under vault/</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Convert crisis events to Obsidian Markdown</name>
<files>vault/危机事件/*.md, vault/危机事件/_index.md</files>
<read_first>
- skills/memory/assets/memory/crisis_knowledge/events/*.json (source data — all 15 JSON files)
- skills/memory/assets/memory/crisis_knowledge/index.json (crisis index)
- .planning/phases/01-obsidian-restructuring/01-CONTEXT.md (decisions D-01 through D-05)
- .agents/skills/obsidian-markdown/SKILL.md (Obsidian format reference)
</read_first>
<behavior>
- Each JSON file becomes one .md file with same event_id as filename
- Frontmatter contains: event_id, event_name, type, severity, event_type, date, status, tags, aliases, affected_markets, key_assets
- Content uses collapsible callouts for detailed sections
- Tags use #危机事件 and #event_type patterns
- Related events referenced via wikilinks
</behavior>
<action>
For each JSON file in `skills/memory/assets/memory/crisis_knowledge/events/`:

1. Read the JSON file completely
2. Create `vault/危机事件/{event_id}.md` with this exact template:

```markdown
=== (frontmatter start)
event_id: "{event_id}"
event_name: "{event_name}"
type: crisis
severity: "{severity}"
event_type: "{event_type}"
date: "{time_period}"
status: historical
tags:
  - 危机事件
  - {event_type}
aliases:
  - {event_name}
affected_markets: {affected_markets array from JSON}
key_assets: {key_assets array from JSON}
=== (frontmatter end)

# {event_name}

{event_details summary - first 2-3 paragraphs from JSON}

> [!info] 基本信息
> - **时间**: {time_period}
> - **类型**: {event_type}
> - **严重程度**: {severity}
> - **关键词**: {keywords joined with ", "}

## 市场影响

> [!details]- 美股影响
> {us_stock content from JSON market_impact}

> [!details]- 港股影响
> {hk_stock content from JSON market_impact}

> [!details]- A股影响
> {a_stock content from JSON market_impact}

## 影响分析

> [!details]- 详细分析
> {impact_analysis from JSON}

## 投资机会

### 短期
{short_term items from JSON investment_opportunities as list}

### 中期
{medium_term items from JSON investment_opportunities as list}

### 长期
{long_term items from JSON investment_opportunities as list}

## 相关事件

{Links to related events using wikilinks like [[CRISIS_XXXX_NAME]] if mentioned in text}
```

3. Create `vault/危机事件/_index.md`:
```markdown
=== (frontmatter start)
title: 危机事件索引
type: index
tags:
  - 索引
=== (frontmatter end)

# 危机事件知识库

收录历史重大危机事件及其投资影响分析。

| 事件ID | 名称 | 类型 | 严重程度 | 时间 |
|--------|------|------|----------|------|
{one table row per event: | {event_id} | {event_name} | {event_type} | {severity} | {time_period} |}
```
</action>
<acceptance_criteria>
- vault/危机事件/ contains exactly as many .md files as there are .json files in skills/memory/assets/memory/crisis_knowledge/events/
- Each .md file has YAML frontmatter with event_id, event_name, severity, event_type fields
- vault/危机事件/_index.md exists and contains a markdown table with header row "| 事件ID | 名称 | 类型 | 严重程度 | 时间 |"
- Tags in frontmatter include "危机事件"
</acceptance_criteria>
<verify>
<automated>ls vault/危机事件/*.md 2>/dev/null | wc -l | xargs test $(ls skills/memory/assets/memory/crisis_knowledge/events/*.json 2>/dev/null | wc -l) -eq && echo "PASS" || echo "FAIL"</automated>
</verify>
<done>All crisis event JSON files converted to Obsidian Markdown with complete frontmatter</done>
</task>

## Verification

- [ ] `vault/` directory exists
- [ ] All 4 category folders created
- [ ] All crisis event JSON files have corresponding .md files
- [ ] Each .md file has valid frontmatter
- [ ] `_index.md` created for crisis events
- [ ] Tags and wikilinks properly formatted

## Success Criteria

- Vault structure matches D-01 specification
- All crisis events converted to Obsidian format
- Frontmatter preserves all JSON fields (D-02)
- Tags and wikilinks implemented (D-03)
- Collapsible callouts used for token optimization (D-05)

## Output

After completion, create `.planning/phases/01-obsidian-restructuring/01-01-SUMMARY.md`
